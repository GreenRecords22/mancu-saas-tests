{
  "name": "mancu-saas-full-audit",
  "version": "1.0.0",
  "scripts": {
    "test": "playwright test",
    "test:report": "playwright test && playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.42.0",
    "faker": "^5.5.3"
  }
}
